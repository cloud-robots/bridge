swagger: "2.0"
info:
  description: "This is the API for cloud-robots bridge server"
  version: "1.0.0"
  title: "cloud-robots bridge server API"
  contact:
    url: "https://github.com/cloud-robots/bridge"
tags:
- name: "subscription"
  description: "subscription services"
- name: "publish"
  description: "publish services"
- name: "messages"
  description: "messages services"
schemes:
- "http"
- "https"
paths:
  /subscribe/:
    put:
      tags:
      - "subscription"
      summary: "put a new subscription"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - in: "body"
        name: "body"
        required: true
        schema:
          $ref: "#/definitions/SubscribeRequest"
      responses:
        201:
          description: "subscription created"
          schema:
            $ref: "#/definitions/SubscribeResponse"
        400:
          description: "bad request"
          schema:
            $ref: "#/definitions/Error"
        500:
          description: "internal server error"
          schema:
            $ref: "#/definitions/Error"
  /subscribe/{subscriber}/:
    get:
      tags:
      - "subscription"
      summary: "get a subscription by subscriber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "subscriber"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/SubscriptionsResponse"
        404:
          description: "subscription not found"
          schema:
            $ref: "#/definitions/Error"
        400:
          description: "bad request"
          schema:
            $ref: "#/definitions/Error"
        500:
          description: "internal server error"
          schema:
            $ref: "#/definitions/Error"
    delete:
      tags:
      - "subscription"
      summary: "delete a subscription by subscriber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "subscriber"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/SubscribeResponse"
        404:
          description: "subscription not found"
          schema:
            $ref: "#/definitions/Error"
        400:
          description: "bad request"
          schema:
            $ref: "#/definitions/Error"
        500:
          description: "internal server error"
          schema:
            $ref: "#/definitions/Error"
  /publish/{topic}/:
    put:
      tags:
      - "publish"
      summary: "publish a new message in a topic"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "topic"
        in: "path"
        required: true
        type: "string"
      - in: "body"
        name: "body"
        required: true
        schema:
          $ref: "#/definitions/Message"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/PublishResponse"
        404:
          description: "subscription not found"
          schema:
            $ref: "#/definitions/Error"
        400:
          description: "bad request"
          schema:
            $ref: "#/definitions/Error"
        500:
          description: "internal server error"
          schema:
            $ref: "#/definitions/Error"
  /messages/{subscriber}/:
    get:
      tags:
      - "messages"
      summary: "get the messages from a subscriber"
      consumes:
      - "application/json"
      produces:
      - "application/json"
      parameters:
      - name: "subscriber"
        in: "path"
        required: true
        type: "string"
      responses:
        200:
          description: "successful operation"
          schema:
            $ref: "#/definitions/MessageResponse"
        404:
          description: "no valid subscriber"
          schema:
            $ref: "#/definitions/Error"
        400:
          description: "bad request"
          schema:
            $ref: "#/definitions/Error"
        500:
          description: "internal server error"
          schema:
            $ref: "#/definitions/Error"
definitions:
  ArrayOfStrings:
    type: "array"
    items:
      type: "string"
  Message:
    type: "object"
    required:
    - subscriber
    - text
    properties:
      subscriber:
        type: "string"
      text:
        type: "string"
  ReadableMessage:
    type: "object"
    required:
    - subscriber
    - text
    - topic
    - time
    properties:
      subscriber:
        type: "string"
      text:
        type: "string"
      topic:
        type: "string"
      time:
        type: "string"
        format: "date-time"
  MessageResponse:
    type: "object"
    required:
    - subscriber
    - messages
    properties:
      subscriber:
        type: "string"
      messages:
        $ref: "#/definitions/ArrayOfReadableMessages"
  ArrayOfReadableMessages:
    type: "array"
    items:
       $ref: "#/definitions/ReadableMessage"
  ArrayOfMessages:
    type: "array"
    items:
       $ref: "#/definitions/Message"
  SubscribeRequest:
    type: "object"
    required:
    - topics
    properties:
      topics:
        $ref: "#/definitions/ArrayOfStrings"
  SubscribeResponse:
    type: "object"
    required:
    - subscriber
    properties:
      subscriber:
        type: "string"
  SubscriptionsResponse:
    type: object
    required:
    - topics
    - subscriber
    properties:
      subscriber:
        type: "string"
      topics:
        $ref: "#/definitions/ArrayOfStrings"
  PublishResponse:
    type: object
    required:
    - message
    properties:
      message:
        type: "string"
  Error:
    type: object
    properties:
      timestamp :
        type: "string"
        format: "date-time"
      status:
        type: "number"
        format: "int32"
      error:
        type: "string"
      message:
        type: "string"
